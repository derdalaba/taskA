package exam.kit.terminal.command;

/**
 * Represents the type of result of a terminal.command.
 * @author uepiy
 */

public enum CommandResultType {
    /**
     * Represents that the command has been executed successfully.
     */
    SUCCESS,
    /**
     * Represents that the command has failed to execute.
     */
    FAILURE
}
